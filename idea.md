## 起始想法
我在做一种研究，在工业场景中比如金融或法律/医疗等涉及到敏感用户信息的大模型训练场景（如Qwen3-32B），在最终上线面向用户的版本中，一般的流程是不允许上线模型吃过真实信息的（即训练数据不允许未经脱敏或匿名化，为了防止模型上线后以某种攻击手段泄漏训练数据），因此存在一种情况，就是我们在测试环境中，基于未脱敏数据做了全套的流程打通、数据合成及模型训练后，需要到生产环境再做一版，这时候就需要对之前的训练数据做脱敏处理后再重新训练，最终上线。但是我们无法确保脱敏是否会影响模型性能，比方说将数据中的人名、地名、公司名、身份证号都给换成假的，会影响多少模型性能，如果会影响性能的话不同脱敏方式会影响多少，以及怎么能力补强？（脱敏手段例如说对与input中的每个实体，都给替换成指令的唯一全局变量）。当前是否有这方面的research研究，我想看看，继续在这上面探索。

细节要求：
- 先前的工作应该有很多了，对比不同脱敏方法对于模型能力的影响这种：包括但不限于基本的只用于推理层面的脱敏，还是说训练数据脱敏但是模型在真实数据上推理这两种，你需要帮我follow，我自己来不及扫盘看related work了
- 最终生产环境是接触不到未脱敏数据的，因此对模型施加的method本身不能包含未脱敏数据（即不能设计任何依赖“真实数据和脱敏数据做对比学习”或“用真实数据提炼知识给学生模型”的方案）
- 为了加厚实验，在测试方法相较于不脱敏的原始数据的性能差异减小之外，还需要反向设置一些安全性角度的测试，比如脱敏的匿名性是不是有所下降容易猜出来，或者经不住提示词越狱之类的
## 

## 需求
你帮我设计一下怎么做才能发篇AAAI，有关于脱敏方式怎么选，选几个怎么对比试验消融，怎么设计方案（哪些指标，脱敏后的task- performance，信息泄露攻击成功率？还是什么），在哪些数据集上测试性能损失以及补强方案要不要提出，以及补强要怎么比还有哪些baseline，咱们探讨一下这个project怎么设计才能发文，主要是讨巧设计，要么工业界有用要么吸睛，不用大而全我也不是发综述，来帮我谋划一下